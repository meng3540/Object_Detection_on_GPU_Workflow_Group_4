# Object_Detection_on_GPU_Workflow_Group_4
Implementation of a real-time Object Detection and Instance Segmentation workflow on the NVIDIA Jetson Orin Nano. This project utilizes TensorRT acceleration and the YOLOv8 model to optimize AI inferencing for high-performance edge computing in mechatronics applications.
Object Detection & Instance Segmentation on GPU Edge DeviceCourse: MENG3540: Parallel ProgrammingInstitution: Humber PolytechnicPresenters: Hrishikesh Patel & Gurshan Singh1. Introduction & Problem StatementThe ChallengeIn modern mechatronics and industrial automation, real-time decision-making is critical. Traditional AI workloads often suffer from high latency when processed on standard CPUs or via cloud-based systems. This project addresses the need for low-latency, on-premise AI inferencing by developing a hardware-accelerated workflow on an embedded edge device.AI Inferencing as a Parallel ProblemAI inferencing is fundamentally a parallel computation problem involving millions of simultaneous matrix multiplications. By utilizing an NVIDIA Jetson GPU, we can execute these operations across hundreds of CUDA cores in parallel rather than sequentially on a CPU, achieving the frame rates required for real-time robotic or drone navigation.2. Hardware & Software SelectionBased on research into real-time embedded applications, we selected the following stack:ComponentSelectionRationaleHardwareNVIDIA Jetson Orin NanoProvides 40 TOPS of AI performance on a low-power, embedded footprint.ModelYOLOv8 (Single-Pass)Industry-standard for balancing speed and accuracy in object detection.FrameworkNVIDIA TensorRTOptimizes model layers for the Ampere GPU architecture to maximize FPS.LibraryPyTorchRobust library for model integration and preprocessing.3. System Workflow & Data PipelineThe following diagram and steps illustrate our complete data flow pipeline:System Block DiagramData Flow PipelineInput Stage: Live Video Stream capture via USB Camera.Preprocessing: Image resizing and normalization on the ARM CPU.Memory Transfer: Host-to-Device (H2D) transfer to Jetson GPU Memory.Inference: Parallel computation using the YOLOv8 TensorRT Engine.Post-processing: Non-Maximum Suppression (NMS) and Bounding Box annotation.Output Stage: Real-time display of annotated frames with FPS Metrics.4. System Integration & CommandsTo overcome OS-level dependency conflicts and achieve hardware acceleration, the following integration steps were performed:Sudo Bridge & Native OS InjectionWe bypassed library conflicts by injecting user paths into the root environment and installing the setproctitle library natively for telemetry:Bash# Bypass Python pip conflicts
sudo -E python3 -m pip install setproctitle

# Sudo Path Injection (in Python script)
sys.path.insert(1, '/home/user/.local/lib/python3.8/site-packages')
TensorRT Acceleration (Cross-Compilation)We cross-compiled the model to a TensorRT engine using FP16 precision to maximize GPU throughput:Bash/usr/src/tensorrt/bin/trtexec --onnx=yolov8n.onnx --saveEngine=yolov8n.engine --fp16
5. Performance Results & ObservationsOur analysis demonstrates the critical need for GPU resources in AI workloads:CPU Baseline (Novice): 2.4 FPS – Severe CPU saturation and high latency.GPU Optimized (Professional): 25.8 FPS – Seamless Tegra GPU integration and low-latency results.Advanced Prototype: Successfully implemented Instance Segmentation at 17.3 FPS, providing pixel-perfect mask generation.6. Project ReflectionConcepts Learned: Hardware-level optimization, CUDA kernel efficiency, and cross-compiling for embedded architectures.Challenges: Overcoming "Dependency Hell" and lib-path conflicts between user and root environments.Career Impact: These skills are directly applicable to my future career in mechatronics and my upcoming co-op at Cenovus Energy.
